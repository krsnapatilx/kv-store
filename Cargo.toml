[package]
name = "kv-store"
version = "0.1.0"
edition = "2024"
authors = ["krsnapatilx"]
description = "kv-store — segmented append-only KV store (learning project)"
license = "MIT"

[dependencies]
crc32fast = "1.3"
