# Example configuration for kv-store

data_dir = "data"
segment_max_bytes = 1048576    # 1 MB
fsync_policy = "always"        # options: "always", "batch"
compaction_threshold = 0.7     # Ratio: compact when > 70 % tombstones